// eslint-disable-next-line
import styles from './gallery-content-renderer.scss';

export function GalleryContentRenderer() {
    const state = {
        type: 'gallery-content-renderer',
    };

    return Object.assign({}, state);
}
