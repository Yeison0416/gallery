// eslint-disable-next-line
import styles from './descriptive-text.scss';

export function DescriptiveText() {
    const state = {
        type: 'descriptive-text',
    };

    return Object.assign({}, state);
}
