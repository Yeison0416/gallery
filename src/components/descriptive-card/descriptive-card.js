// eslint-disable-next-line
import styles from './descriptive-card.scss';

export function DescriptiveCard() {
    const state = {
        type: 'descriptive-card',
    };

    return Object.assign({}, state);
}
